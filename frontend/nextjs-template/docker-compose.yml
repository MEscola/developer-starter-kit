services:
  frontend:
    container_name: nextjs-template

    build: .

    restart: unless-stopped

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules

    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000